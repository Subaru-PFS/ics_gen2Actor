setupRequired(spt_operational_database)
setupRequired(tron_actorcore)
setupRequired(ics_actorkeys)
setupRequired(pfs_utils)

pathPrepend(PYTHONPATH, ${PRODUCT_DIR}/g2cam)
pathPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)

pathPrepend(PATH, ${PRODUCT_DIR}/g2cam/scripts)
pathPrepend(PATH, ${PRODUCT_DIR}/bin)
